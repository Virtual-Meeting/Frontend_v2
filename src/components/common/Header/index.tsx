import {
    HeaderWrapper,
    LogoImage
} from './Header.styles';

const Header: React.FC = () => {
    return(
        <HeaderWrapper>
            <LogoImage />
        </HeaderWrapper>
    );
}

export default Header;